# Experiment Results

This directory contains JSONL output files and summary reports from verifier loop experiments.

Files are generated by `run_experiment.py` and `compute_metrics.py`. They are gitignored.
